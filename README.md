bootstrap-tooltip-parent
========================

With this script you can add a parent value for bootstrap tooltips (v2.3.1)

Example:
$('#undoButton').tooltip({placement:'right',title:'Undo', parent: '#redoButton'});
